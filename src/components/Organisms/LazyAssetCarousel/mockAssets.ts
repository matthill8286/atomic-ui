import { Asset } from '@/types'
import { newAssetMocks } from '@/components/Organisms/AssetTile'

export const mockAsset: Asset = newAssetMocks[0]

export const loveFilmAsset: Asset = newAssetMocks[1]

export const microsoftAsset: Asset = newAssetMocks[2]
